# Runtime lane (.venv, created by scripts/setup.*). Maqueta runs no Python at request time (the web app is a static
# replay), so this only pins the pipeline code's own runtime dependency. Verified on Python 3.12.
numpy==2.4.6
