# Pipeline lane, installed by CI and scripts/setup.* into .venv-pipeline. It pins what the offline tests, the
# index regeneration and the guards need; none of them touch the network. Baking places (and gen_admin) also
# needs requirements-bake.txt in this folder: the geoscena core pinned to the commit the bundles match.
numpy==2.4.6
